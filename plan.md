1. **Add `aria-label` and `title` to AdminPage.js icon buttons**
   - The close profile modal button `✕` needs an `aria-label="إغلاق" title="إغلاق"`
   - The delete class button `🗑️` needs an `aria-label="حذف الفصل" title="حذف الفصل"`
   - The close class modal button `✕` needs an `aria-label="إغلاق" title="إغلاق"`
2. **Complete pre commit steps**
   - Call `pre_commit_instructions` to make sure proper testing, verifications, reviews and reflections are done.
3. **Submit the change.**
   - Once all checks pass, I will submit the change with a descriptive commit message.
